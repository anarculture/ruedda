# repo publico: solo lo servible; material interno queda local
*
!.gitignore
# la home: el landing publico
!index.html
# el dominio propio, lo lee GitHub Pages
!CNAME
# imagen de vista previa al compartir el link
!og.png
# el deck de inversionistas, en su propia ruta
!pitch-deck/
!pitch-deck/index.html
# ilustraciones y marca del landing, heredadas del app (ruedda-platform/frontend/assets/images)
!assets/
!assets/*.webp
!assets/*.png
